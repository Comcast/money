money{
  enabled = false
  bizzare-setting = "bozo"

  emitter{
    emitters = [
      {
        name = "log-emitter"
        class-name = "com.comcast.money.emitters.LogRecorder"
        subscriptions = [Trace]
        configuration = {
        }
      }
    ]
  }
}